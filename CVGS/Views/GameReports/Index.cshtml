@model IEnumerable<CVGS.Models.Game>
@{
    ViewData["Title"] = "Games Reports";
}
@{
    ViewData["dateTime"] = DateTime.Now;
}

@using Microsoft.AspNetCore.Http
@{
    if (Context.Session.GetString("isEmp") == "True")
    {
        <div class="container no-padding">
            <div class="row">
                <div class="col-md-3">
                    @await Html.PartialAsync("_AdminPortalNavBar")
                </div>
                <div class="col-md-9">
                    <div class="container">
                        <div class="row">
                            <h1 id="gamereports-header">@ViewData["Title"]</h1>
                        </div>
                    </div>
                    <table class="table">
                        <thead>
                            <tr>
                                <th>
                                    Game Name
                                </th>
                                <th>
                                    @Html.DisplayNameFor(model => model.Category)
                                </th>
                                <th>
                                    @Html.DisplayNameFor(model => model.Platform)
                                </th>
                                <th></th>
                            </tr>
                        </thead>
                        <tbody>
                            @foreach (var item in Model)
                            {
                                <tr>
                                    <td>
                                        @Html.DisplayFor(modelItem => item.Name)
                                    </td>
                                    <td>
                                        @Html.DisplayFor(modelItem => item.Category.Category)
                                    </td>
                                    <td>
                                        @Html.DisplayFor(modelItem => item.Platform.Platform)
                                    </td>
                                </tr>
                            }
                    </table>

                    @ViewData["dateTime"]
                </div>
            </div>
        </div>
    }
    else
    {
        <h1>Forbidden</h1>
        <p>You are not authorized to access this feature of the website.</p>
        <a asp-controller="Home" asp-action="Index">Back to Home</a>
    }
}

